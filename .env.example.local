# Local/Development Environment Example
# Copy this file to .env for local development

# General Settings
TAG=latest
SECRET_KEY=your_dev_secret_key_here
ALLOWED_ORIGINS=*
BASE_URL=http://localhost

# Redis Configuration
REDIS_URL=redis://redis:6379/0

# Database Configuration
POSTGRES_DB=miningcore
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
# For local development using Docker, use 'host.docker.internal' to connect to PostgreSQL on your host machine
# If PostgreSQL is in another container, use the service name (e.g., 'postgres')
POSTGRES_HOST=host.docker.internal
POSTGRES_PORT=5432

# API Configuration
# For local development pointing to the API server
API_BASE_URL=http://5.78.102.130:8000

# Schedule Configuration (in hours)
UPDATE_INTERVAL_HOURS=3

# SSL Configuration (not typically used in development)
DOMAIN_NAME=localhost
EMAIL_FOR_SSL=dev@example.com 